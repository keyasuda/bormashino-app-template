{
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.7.0",
    "prettier": "^2.6.2"
  },
  "source": "js/index.html",
  "scripts": {
    "dev": "parcel --dist-dir tmp/",
    "build": "bundle exec rake && parcel build --no-scope-hoist"
  },
  "dependencies": {
    "bormashino": "^0.1.9"
  }
}
